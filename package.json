{
  "name": "lto-implementacion",
  "private": true,
  "description": "Raíz del repo; la app Next.js vive en reestructura-app/.",
  "scripts": {
    "install:app": "npm install --prefix reestructura-app",
    "dev": "npm run dev --prefix reestructura-app",
    "clean": "npm run clean --prefix reestructura-app",
    "build": "npm run build --prefix reestructura-app",
    "lint": "npm run lint --prefix reestructura-app",
    "test": "npm run test --prefix reestructura-app"
  }
}
