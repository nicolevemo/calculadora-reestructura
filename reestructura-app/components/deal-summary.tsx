/** Sprint 3: resumen del acuerdo (tabla de métricas). */
export function DealSummaryPlaceholder() {
  return (
    <div className="rounded-lg border border-dashed p-6 text-center text-sm text-muted-foreground">
      <code className="text-xs">DealSummary</code> — Sprint 3
    </div>
  );
}
