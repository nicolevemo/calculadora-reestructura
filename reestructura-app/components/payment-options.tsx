/** Sprint 3: presets de pago de intención + input estable (sin perder foco). */
export function PaymentOptionsPlaceholder() {
  return (
    <div className="rounded-lg border border-dashed p-6 text-center text-sm text-muted-foreground">
      <code className="text-xs">PaymentOptions</code> — Sprint 3
    </div>
  );
}
