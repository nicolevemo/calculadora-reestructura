/** Sprint 2: tabla del dashboard (agente). */
export function ClientTablePlaceholder() {
  return (
    <div className="rounded-lg border border-dashed p-8 text-center text-sm text-muted-foreground">
      <code className="text-xs">ClientTable</code> — Sprint 2
    </div>
  );
}
