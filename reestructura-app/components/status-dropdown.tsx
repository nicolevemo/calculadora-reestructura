/** Sprint 3: dropdown de estado + reglas de intentos / motivo / fecha. */
export function StatusDropdownPlaceholder() {
  return (
    <div className="rounded-md border border-dashed px-3 py-2 text-center text-xs text-muted-foreground">
      StatusDropdown — Sprint 3
    </div>
  );
}
